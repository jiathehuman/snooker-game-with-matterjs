/*-------------------------------------------------------
GAME STATES
There should be three game states for the game:
1. When player is placing the cueball
2. When player hits the cueball
---------------------------------------------------------*/

// When player is placing the cueball, nothing else should move.
function placingCueBall()
{
    // for(var i = 0; i < colouredBalls.length;i++)
    // Matter.Body.set(colouredBalls[i].body,"isStatic",true)
    // console.log(mouseConstraint)
    // console.log(mouseConstraint.collisionFilter.group)
}